import { Result } from 'antd';

const About = () => {
  return (
    <Result
      status="info"
      title={'About'}
      subTitle={'Simple ERP / Accounting system running on your own server.'}
    />
  );
};

export default About;
